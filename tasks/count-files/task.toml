schema_version = "1.1"

[task]
name = "count-files"
description = "Create exactly 3 .txt files in a directory"

[agent]
timeout_sec = 120

[verifier]
timeout_sec = 30

[environment]
cpus = 1
memory_mb = 512
