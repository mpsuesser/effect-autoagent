schema_version = "1.1"

[task]
name = "simple-script"
description = "Write and run a Python script that computes sum(1..100)"

[agent]
timeout_sec = 120

[verifier]
timeout_sec = 30

[environment]
cpus = 1
memory_mb = 512
